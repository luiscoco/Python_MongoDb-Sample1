# Python_MongoDb-Sample1

1. First run the Mongo docker container:
```
docker run -d -p 27017:27017 --name mongodb mongo
```

2. Then open VSCode and navigate to the main.py file and run the application:
```
python main.py
```
